interface:
  display_name: "Decision Recon"
  short_description: "Research a reversible engineering decision"
  default_prompt: "Use $decision-recon to compare these engineering options with hard gates, current evidence, sensitivity, and reversal conditions."
policy:
  allow_implicit_invocation: false
