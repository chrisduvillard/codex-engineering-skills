interface:
  display_name: "Harvest Agent Branches"
  short_description: "Recover coherent work from divergent branches"
  default_prompt: "Use $harvest-agent-branches to compare the source and target, salvage coherent slices, verify them, and preserve recovery."
policy:
  allow_implicit_invocation: false
