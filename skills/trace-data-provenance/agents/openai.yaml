interface:
  display_name: "Trace Data Provenance"
  short_description: "Trace one value across semantic boundaries"
  default_prompt: "Use $trace-data-provenance to trace this value end to end and identify the first unproven or violated semantic boundary."
policy:
  allow_implicit_invocation: true
