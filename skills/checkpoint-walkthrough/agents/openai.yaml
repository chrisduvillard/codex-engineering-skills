interface:
  display_name: "Checkpoint Walkthrough"
  short_description: "Guide a bounded human review decision"
  default_prompt: "Use $checkpoint-walkthrough to explain this change, identify high-value review stops, and prepare an approve, rework, or discuss decision."
policy:
  allow_implicit_invocation: false
