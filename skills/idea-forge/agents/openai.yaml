interface:
  display_name: "Idea Forge"
  short_description: "Pressure-test an idea before planning"
  default_prompt: "Use $idea-forge to clarify this idea, attack its weakest assumptions, defend what survives, and render an honest outcome."
policy:
  allow_implicit_invocation: false
