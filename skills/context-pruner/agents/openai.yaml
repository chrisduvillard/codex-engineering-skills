interface:
  display_name: "Context Pruner"
  short_description: "Reduce repository agent context debt safely"
  default_prompt: "Use $context-pruner to audit the repository's agent instructions, verify loading and evidence, and propose or apply safe reductions."
policy:
  allow_implicit_invocation: false
