interface:
  display_name: "Brownfield Steward"
  short_description: "Maintain durable evidence-backed project memory"
  default_prompt: "Use $steward-brownfield to initialize or resume durable project memory and perform the requested bounded brownfield workflow."
policy:
  allow_implicit_invocation: false
